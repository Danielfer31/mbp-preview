---
phase: 01-public-mvp
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/pages/index.astro
  - src/pages/perfil.astro
  - src/pages/servicios.astro
  - src/pages/contacto.astro
  - src/pages/privacidad.astro
  - src/pages/404.astro
  - src/components/ServiceCard.astro
  - src/components/SafeContactCard.astro
  - src/components/LocationsHours.astro
  - tests/e2e/public-pages.spec.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-07, WEB-10, CNT-01, CNT-02, PRV-01, PRV-02, PRV-07]
user_setup: []

must_haves:
  truths:
    - "Las paginas publicas solo muestran datos no inventados, leidos del contenido estructurado"
    - "Se presentan perfil, servicios, sedes, horarios y contacto (WEB-01)"
    - "Los CTA sin destino real estan deshabilitados con aria-disabled y razon visible"
    - "Un canal alternativo validado permanece visible cuando el canal principal no esta disponible (WEB-10)"
    - "Existe pagina 404 personalizada con enlace al inicio"
    - "El disclaimer de no enviar informacion clinica aparece junto a cada CTA de contacto (WEB-07)"
  artifacts:
    - path: "src/components/LocationsHours.astro"
      provides: "Render de sedes y horarios desde locations.json/hours.json (WEB-01)"
      min_lines: 20
    - path: "src/components/SafeContactCard.astro"
      provides: "CTA con estados habilitado/deshabilitado y disclaimer; logica de canal alternativo"
      contains: "aria-disabled"
    - path: "src/pages/404.astro"
      provides: "Pagina 404 personalizada con enlace al home"
      contains: "no existe"
  key_links:
    - from: "src/pages/servicios.astro"
      to: "src/content/site/services.json"
      via: "getCollection/getEntry"
      pattern: "services|getCollection"
    - from: "src/components/SafeContactCard.astro"
      to: "src/lib/placeholders.ts"
      via: "estado de placeholder determina disabled"
      pattern: "placeholder|BLOCKING"
---

<objective>
Construir las paginas y secciones publicas leyendo exclusivamente del contenido
estructurado, cerrando MEDIUM concerns:
- Sedes y horarios renderizados en pagina con su componente (WEB-01).
- Comportamiento de canal alternativo definido: cuando el canal principal no esta disponible
  (placeholder bloqueante activo), un canal alternativo validado permanece visible (WEB-10);
  si tampoco hay canal validado, se muestra el estado seguro sin enlaces ficticios.
- Pagina 404 personalizada con enlace al inicio (concern LOW Codex).

Purpose: experiencia completa revisable sin datos inventados, con contacto humano seguro.

Output: 5 paginas + 404, ServiceCard, SafeContactCard, LocationsHours y E2E de paginas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-public-mvp/01-UI-SPEC.md
@.planning/phases/01-public-mvp/01-SPEC.md
@.planning/content/PLACEHOLDER-REGISTER.md
@.planning/content/MBP-BRAND-ANALYSIS.md
@.planning/phases/01-public-mvp/01-02-SUMMARY.md
@.planning/phases/01-public-mvp/01-03-SUMMARY.md

<!-- BRAND: Para estilos visuales de componentes (ServiceCard, SafeContactCard, layouts de página)
     invocar skill mbp-brand-design y consultar MBP-BRAND-ANALYSIS.md.
     Secciones clave: "COMPONENTES DETALLADOS", "ESTRUCTURA DE PÁGINA", "FOTOGRAFÍA".
     Logo en Header: SVG placeholder (bloqueante confirmado — SVG definitivo pendiente del diseñador).
     Botones: siempre pill (border-radius: 9999px). Cards: radius 14px. Fondo hero: #DCCDFF. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componentes de servicio, contacto seguro y sedes/horarios</name>
  <read_first>
    - .planning/content/MBP-BRAND-ANALYSIS.md — secciones "COMPONENTES DETALLADOS" y "ESTRUCTURA DE PÁGINA" (LEER PRIMERO para estilos visuales)
    - Invocar skill mbp-brand-design antes de estilizar cualquier componente
    - .planning/phases/01-public-mvp/01-UI-SPEC.md (ServiceCard, SafeContactCard, Copywriting Contract, Interaction States — estructura y semántica; los colores vienen del MBP-BRAND-ANALYSIS)
    - .planning/content/PLACEHOLDER-REGISTER.md (CTA deshabilitados, canal alternativo, WEB-10)
    - src/lib/placeholders.ts (BLOCKING_PLACEHOLDER_IDS, de 01-02)
    - src/content/site/locations.json, src/content/site/hours.json (de 01-02)
  </read_first>
  <files>src/components/ServiceCard.astro, src/components/SafeContactCard.astro, src/components/LocationsHours.astro</files>
  <action>
    Crear `src/components/ServiceCard.astro`: muestra el nombre confirmado del servicio;
    si la descripcion esta en placeholder (PH-SERVICE-DESCRIPTIONS bloqueante) muestra solo
    el nombre, sin descripciones medicas no aprobadas. Estilo de tarjeta del UI-SPEC.
    Crear `src/components/SafeContactCard.astro`: recibe un canal (telefono/email/whatsapp).
    Si el placeholder del canal es bloqueante/no resuelto => CTA deshabilitado con
    `aria-disabled="true"`, `title` = "Este enlace estara disponible cuando se confirme la
    informacion de contacto." y texto "Informacion de contacto proximamente"; NUNCA emite href
    ficticio. Si esta resuelto => enlace real (tel:/mailto:/wa.me) con esquema validado. Logica
    de canal alternativo (WEB-10): si el canal principal no esta disponible, mostrar de forma
    visible el canal alternativo cuando PH-ALTERNATE-CHANNEL este resuelto; si tampoco lo esta,
    mostrar texto seguro "Canal alternativo: Informacion disponible proximamente". Incluir junto
    a cada CTA el disclaimer corto exacto "No envie informacion clinica por este canal." en
    --color-destructive. Crear `src/components/LocationsHours.astro`: renderiza sedes desde
    locations.json y horarios desde hours.json; si estan en placeholder, muestra "Informacion
    disponible proximamente" en lugar de inventar direccion/horario (WEB-01).
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/components/SafeContactCard.astro','utf8'); if(!/aria-disabled/.test(c))throw new Error('falta aria-disabled'); if(!/No envie informacion clinica por este canal/.test(c))throw new Error('falta disclaimer CTA'); const l=require('fs').readFileSync('src/components/LocationsHours.astro','utf8'); if(!/hours|horario/i.test(l))throw new Error('falta horarios'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - SafeContactCard.astro: CTA deshabilitado usa aria-disabled="true", title exacto y sin href ficticio cuando el placeholder bloquea
    - SafeContactCard.astro incluye el disclaimer corto exacto junto al CTA
    - SafeContactCard.astro muestra canal alternativo visible cuando el principal no esta disponible (WEB-10)
    - LocationsHours.astro lee de locations.json y hours.json y degrada a texto seguro en placeholder
    - ServiceCard.astro muestra solo nombre cuando la descripcion esta en placeholder
  </acceptance_criteria>
  <done>Componentes que renderizan contacto, servicios, sedes y horarios de forma segura, con CTA deshabilitados, canal alternativo y disclaimers.</done>
</task>

<task type="auto">
  <name>Task 2: Paginas publicas, 404 personalizada y E2E de rutas</name>
  <read_first>
    - .planning/phases/01-public-mvp/01-UI-SPEC.md (Copywriting Contract, empty states, error 404)
    - src/components/ServiceCard.astro, SafeContactCard.astro, LocationsHours.astro (Task 1)
    - src/content/site/*.json (de 01-02)
  </read_first>
  <files>src/pages/index.astro, src/pages/perfil.astro, src/pages/servicios.astro, src/pages/contacto.astro, src/pages/privacidad.astro, src/pages/404.astro, tests/e2e/public-pages.spec.ts</files>
  <action>
    Crear las 5 paginas usando BaseLayout y leyendo del contenido estructurado via
    getCollection/getEntry (NO hardcodear afirmaciones — esto sera verificado por el CI check
    del plan 01-05). index: hero con nombre/propuesta aprobada y CTA. perfil: experiencia y
    credenciales solo si estan aprobadas; empty state "Las credenciales verificadas se
    publicaran proximamente." cuando esten pendientes. servicios: grid de ServiceCard; empty
    state "Los servicios disponibles se publicaran cuando esten confirmados." contacto: usa
    SafeContactCard + LocationsHours; muestra sedes, horarios, canales y canal alternativo.
    privacidad: condiciones de contacto, advertencia clinica, responsable de privacidad
    (placeholder si pendiente) y procedimiento ante mensajes clinicos/urgencias en texto
    preventivo. Crear `src/pages/404.astro` con texto exacto "Esta pagina no existe. Puede
    regresar al inicio o contactar directamente." y enlace al home. Escribir
    `tests/e2e/public-pages.spec.ts` que recorre las 5 rutas + 404, verifica encabezados de
    pagina, presencia de empty states/placeholders esperados y que ningun CTA bloqueado tenga
    href real.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('src/pages/404.astro','utf8'); if(!/Esta pagina no existe/.test(f))throw new Error('falta copy 404'); console.log('ok')" && npm run build && npx playwright test tests/e2e/public-pages.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Existen index, perfil, servicios, contacto, privacidad y 404
    - Cada pagina renderiza datos via getCollection/getEntry, sin valores medicos hardcodeados
    - 404.astro contiene el copy exacto y un enlace a "/"
    - tests/e2e/public-pages.spec.ts pasa para las 6 rutas
    - El E2E verifica que un CTA en estado bloqueado no expone href real
  </acceptance_criteria>
  <done>Experiencia publica completa revisable con sedes/horarios/contacto, canal alternativo, 404 personalizada y sin datos inventados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenido estructurado -> HTML publico | datos no aprobados no deben renderizarse como verificados |
| sitio -> terceros (tel/mailto/wa.me) | el visitante sale a apps externas donde podria revelar datos de salud |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Spoofing | CTA con href ficticio | mitigate | CTA deshabilitado sin href cuando el placeholder bloquea |
| T-01-12 | Information Disclosure | salto a canal externo con datos clinicos | mitigate | disclaimer junto a cada CTA; canal alternativo visible (WEB-10) |
| T-01-13 | Repudiation | afirmacion medica sin aprobacion | mitigate | render solo desde contenido estructurado aprobado (CNT-01) |
</threat_model>

<verification>
- E2E de las 6 rutas verde.
- CTA bloqueados sin href; canal alternativo visible.
- Sedes y horarios presentes en /contacto.
</verification>

<success_criteria>
- WEB-01/WEB-02/WEB-10 cubiertos en paginas reales.
- 404 personalizada.
- Sin datos inventados.
</success_criteria>

<output>
Crear `.planning/phases/01-public-mvp/01-04-SUMMARY.md` al terminar.
</output>
