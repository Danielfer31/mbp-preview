---
phase: 01-public-mvp
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/components/SeoHead.astro
  - src/pages/robots.txt.ts
  - src/pages/sitemap.xml.ts
  - public/_headers
  - public/_routes.json
  - scripts/check-hardcoded-content.mjs
  - tests/unit/seo.test.ts
  - tests/e2e/privacy.spec.ts
autonomous: true
requirements: [WEB-06, WEB-07, WEB-08, WEB-09, PRV-01, PRV-04, CNT-01, QA-03]
user_setup: []

must_haves:
  truths:
    - "Un CI check rechaza afirmaciones medicas, contacto, metadata o schema hardcodeados en archivos .astro"
    - "Todas las afirmaciones publicas provienen exclusivamente del contenido estructurado"
    - "Los metadatos SEO no usan direccion, telefono o dominio ficticios"
    - "El schema medico solo se emite tras verificacion de credenciales (no en este plan)"
    - "Los headers de seguridad (CSP, Referrer-Policy, Permissions-Policy, nosniff) estan definidos para Cloudflare Pages"
    - "El HTML generado no contiene formularios, cookies, almacenamiento ni scripts de analitica"
  artifacts:
    - path: "scripts/check-hardcoded-content.mjs"
      provides: "CI check que detecta contenido prohibido hardcodeado en src/ y public/ (H2)"
      contains: "glob\|readdir\|.ts\|.js\|public"
    - path: "public/_headers"
      provides: "Headers de seguridad para Cloudflare Pages"
      contains: "Content-Security-Policy"
    - path: "public/_routes.json"
      provides: "Control de rutas estaticas en Cloudflare Pages"
      min_lines: 1
  key_links:
    - from: "scripts/check-hardcoded-content.mjs"
      to: "src/**/*.{astro,ts,js,mjs}"
      via: "scan for forbidden hardcoded claims"
      pattern: "glob\|readdir"
    - from: "src/components/SeoHead.astro"
      to: "src/content/site"
      via: "metadata desde contenido estructurado"
      pattern: "content|getEntry"
---

<objective>
Implementar SEO tecnico, headers de seguridad estaticos y, sobre todo, el CI CHECK QUE
IMPIDE CONTENIDO HARDCODEADO (cierra concern HIGH H2): el plan ahora previene que
afirmaciones medicas, datos de contacto, metadata o schema se incrusten directamente en
archivos .astro evadiendo el sistema de contenido/validador.

Tambien cierra concerns LOW/MEDIUM: _routes.json y 404 en contexto estatico, omision de
schema medico hasta verificacion de credenciales (se habilita en 01-08).

Purpose: sitio rastreable y seguro donde toda afirmacion publica proviene del contenido
estructurado verificable.

Output: SeoHead, robots, sitemap, _headers, _routes.json, check-hardcoded-content y pruebas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-public-mvp/01-UI-SPEC.md
@.planning/phases/01-public-mvp/01-SPEC.md
@.planning/content/PLACEHOLDER-REGISTER.md
@.planning/phases/01-public-mvp/01-02-SUMMARY.md
@.planning/phases/01-public-mvp/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CI check anti-hardcodeo de contenido medico/contacto/metadata/schema</name>
  <read_first>
    - .planning/content/CONTENT-INVENTORY.md (terminos medicos, credenciales, ciudad, direccion candidatos)
    - .planning/content/PLACEHOLDER-REGISTER.md (campos que deben venir del contenido estructurado)
    - .planning/phases/01-public-mvp/01-REVIEWS.md (H2: prevenir afirmaciones hardcodeadas en .astro)
  </read_first>
  <files>scripts/check-hardcoded-content.mjs, tests/unit/seo.test.ts</files>
  <behavior>
    - Falla (exit != 0) si cualquier archivo en src/ o public/ contiene afirmacion medica/credencial prohibida fuera del contenido estructurado (p.ej. numeros de experiencia, "especialista en", nombres de servicios con descripcion clinica)
    - Falla si src/**/*.{astro,ts,js,mjs} o public/ contiene telefono literal, email literal, URL wa.me literal o direccion postal literal
    - Falla si cualquier archivo inyecta JSON-LD/schema.org con datos literales en vez de leerlos del contenido estructurado
    - Falla si robots.txt.ts o sitemap.xml.ts contiene contacto, afirmacion medica o credencial literal
    - Pasa cuando los archivos solo referencian datos via getCollection/getEntry/props/import desde content/
    - El check es invocable como `node scripts/check-hardcoded-content.mjs` y reporta archivo+linea de cada violacion
  </behavior>
  <action>
    Crear `scripts/check-hardcoded-content.mjs` que escanee con glob todos los archivos bajo
    `src/` (extension .astro, .ts, .js, .mjs) y `public/` (archivos de texto: .html, .js, .json)
    buscando: (a) patrones de telefono colombiano y E.164, direcciones de email, URLs
    `wa.me`/`whatsapp`, direcciones postales literales; (b) afirmaciones medicas/credenciales
    prohibidas derivadas de CONTENT-INVENTORY (regex sobre terminos como "anos de experiencia"
    con digitos, "especialista en", "registro profesional", nombres de servicios seguidos de
    descripcion clinica); (c) bloques `application/ld+json` con valores literales en lugar de
    interpolacion desde contenido. Usar `fs.readdirSync` recursivo o `glob` (sin dependencias
    extra — Node 18+ soporta `glob` nativo). Excluir `src/content/` (ahi vive el contenido
    aprobado) y `node_modules/`. Permitir explicitamente referencias via `getCollection`,
    `getEntry`, `Astro.props` y variables de import desde content/. Reportar cada violacion con
    ruta y numero de linea y salir != 0 si hay alguna. Escribir `tests/unit/seo.test.ts` con
    fixtures: un .astro con telefono hardcodeado debe fallar; un .astro que solo usa getEntry
    debe pasar; un .astro con JSON-LD literal debe fallar; un .ts con email literal debe fallar.
  </action>
  <verify>
    <automated>npm test -- tests/unit/seo.test.ts && node scripts/check-hardcoded-content.mjs</automated>
  </verify>
  <acceptance_criteria>
    - check-hardcoded-content.mjs sale != 0 ante telefono/email/wa.me/direccion literal en .astro
    - check-hardcoded-content.mjs sale != 0 ante telefono/email/credencial literal en .ts, .js o .mjs bajo src/
    - check-hardcoded-content.mjs sale != 0 ante afirmacion medica en robots.txt.ts o sitemap.xml.ts
    - check-hardcoded-content.mjs sale != 0 ante JSON-LD con valores literales en cualquier archivo escaneado
    - check-hardcoded-content.mjs sale 0 cuando los archivos solo usan getCollection/getEntry/props/import desde content/
    - check-hardcoded-content.mjs excluye src/content/ (donde vive el contenido aprobado)
    - Cada violacion reporta archivo y numero de linea
    - tests/unit/seo.test.ts pasa con los cuatro fixtures (telefono en .astro falla, getEntry pasa, JSON-LD literal falla, email en .ts falla)
  </acceptance_criteria>
  <done>CI check operativo que impide evadir el sistema de contenido hardcodeando afirmaciones en src/ y public/ (H2 cerrado completamente).</done>
</task>

<task type="auto">
  <name>Task 2: SeoHead, robots, sitemap, headers de seguridad y _routes.json</name>
  <read_first>
    - .planning/phases/01-public-mvp/01-UI-SPEC.md (Privacy & No-Tracking Contract, Performance Contract)
    - .planning/content/PLACEHOLDER-REGISTER.md (PH-CITY, PH-DOMAIN, PH-ADDRESS no inventar)
    - src/content/site/*.json (metadatos desde contenido, de 01-02)
  </read_first>
  <files>src/components/SeoHead.astro, src/pages/robots.txt.ts, src/pages/sitemap.xml.ts, public/_headers, public/_routes.json, tests/e2e/privacy.spec.ts</files>
  <action>
    Crear `src/components/SeoHead.astro` que genere title/description/canonical/OG leyendo del
    contenido estructurado; usar la meta description placeholder "Sitio profesional de medicina
    pediatrica. Informacion en proceso de verificacion." mientras PH-CITY/PH-DOMAIN sean
    bloqueantes; NO emitir schema medico (MedicalBusiness/Physician con medicalSpecialty)
    todavia — eso se habilita en 01-08 tras verificacion de credenciales (Gemini suggestion +
    H5). Crear `src/pages/robots.txt.ts` y `src/pages/sitemap.xml.ts` (endpoints estaticos).
    Crear `public/_headers` (formato Cloudflare Pages) con: Content-Security-Policy que bloquee
    `script-src` externo por defecto (self + unsafe-inline solo si imprescindible para el toggle
    inline, preferir hash), `Referrer-Policy: no-referrer`, `Permissions-Policy` restrictiva,
    `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`. Crear `public/_routes.json`
    para servir todo como estatico (sin Functions) y evitar errores de crawl. Escribir
    `tests/e2e/privacy.spec.ts` que verifique sobre el build: ausencia de `<form>`, ausencia de
    cookies/localStorage/sessionStorage, ausencia de scripts de analitica/terceros, y presencia
    de robots.txt y sitemap.xml.
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('public/_headers','utf8'); for(const k of ['Content-Security-Policy','Referrer-Policy','Permissions-Policy','X-Content-Type-Options']) if(!h.includes(k)) throw new Error('falta '+k); console.log('ok')" && npm run build && npx playwright test tests/e2e/privacy.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - public/_headers contiene Content-Security-Policy, Referrer-Policy, Permissions-Policy y X-Content-Type-Options: nosniff
    - SeoHead.astro NO emite schema MedicalBusiness/Physician en este plan
    - SeoHead.astro toma metadatos del contenido estructurado, sin dominio/telefono/direccion ficticios
    - public/_routes.json existe y declara servicio estatico
    - tests/e2e/privacy.spec.ts pasa: sin form, sin storage, sin analitica, con robots y sitemap
  </acceptance_criteria>
  <done>SEO tecnico y headers de seguridad listos; schema medico diferido a verificacion; privacidad sin tracking verificada en build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .astro source -> HTML publico | un dev podria hardcodear afirmaciones evadiendo el validador (H2) |
| navegador -> sitio estatico | CSP y headers limitan ejecucion y fuga de datos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Tampering | afirmaciones hardcodeadas en .astro (H2) | mitigate | check-hardcoded-content.mjs en CI bloquea telefono/email/medico/JSON-LD literal |
| T-01-15 | Information Disclosure | scripts de terceros / analitica | mitigate | CSP bloquea script-src externo; privacy.spec verifica ausencia |
| T-01-16 | Spoofing | schema medico sin credenciales verificadas | mitigate | schema diferido hasta 01-08 tras verificacion |
| T-01-17 | Tampering | clickjacking | mitigate | X-Frame-Options: DENY y frame-ancestors en CSP |
</threat_model>

<verification>
- check-hardcoded-content sale 0 contra el codigo actual.
- privacy.spec verde.
- _headers contiene los headers requeridos.
</verification>

<success_criteria>
- H2 cerrado: no se puede hardcodear contenido prohibido.
- SEO sin datos ficticios; schema medico diferido.
- Headers de seguridad definidos.
</success_criteria>

<output>
Crear `.planning/phases/01-public-mvp/01-05-SUMMARY.md` al terminar.
</output>
