---
phase: 01-public-mvp
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/styles/tokens.css
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/PlaceholderNotice.astro
  - tests/e2e/layout.spec.ts
autonomous: true
requirements: [WEB-03, WEB-04, WEB-05, WEB-07, QA-03, QA-04]
user_setup: []

must_haves:
  truths:
    - "El documento declara lang=es en el elemento html (WCAG + SEO)"
    - "El disclaimer clinico es un elemento visible persistente en el footer de cada pagina (no solo metadata)"
    - "Existe skip link, foco visible en todos los interactivos y soporte de prefers-reduced-motion"
    - "El layout es responsive mobile-first con landmarks ARIA correctos"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Layout base con lang=es, skip link, main#main-content, landmarks"
      contains: "lang=\"es\""
    - path: "src/components/Footer.astro"
      provides: "Footer con disclaimer clinico persistente visible"
      contains: "No envie"
    - path: "src/styles/tokens.css"
      provides: "CSS custom properties del UI-SPEC"
      contains: "--color-accent"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Footer.astro"
      via: "import and render on every page"
      pattern: "Footer"
    - from: "src/styles/global.css"
      to: "src/styles/tokens.css"
      via: "import tokens"
      pattern: "tokens"
---

<objective>
Construir el sistema visual y el layout base conforme al contrato 01-UI-SPEC.md, con tres
correcciones de review:
- lang="es" en BaseLayout.astro (flag Gemini: requisito WCAG + SEO).
- Disclaimer clinico como elemento VISIBLE PERSISTENTE en el footer de cada pagina, no solo
  metadata (concern MEDIUM ambos revisores, Ley 23/1981 Colombia).
- Foco visible, skip link y prefers-reduced-motion sin movimiento por defecto.

Purpose: base accesible, ligera y profesional sobre la que se montan las paginas publicas.

Output: tokens CSS, layout, Header, Footer (con disclaimer), PlaceholderNotice y E2E de
layout/accesibilidad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-public-mvp/01-UI-SPEC.md
@.planning/phases/01-public-mvp/01-SPEC.md
@.planning/phases/01-public-mvp/01-01-SUMMARY.md
@.planning/content/MBP-BRAND-ANALYSIS.md

<!-- BRAND AUTHORITY: Los tokens de color en este plan provienen del PDF de branding oficial
     (MBP-BRAND-ANALYSIS.md), NO del UI-SPEC previo. El UI-SPEC definía --color-accent #C17A4A
     (terracota) que NO existe en la paleta real. MBP-BRAND-ANALYSIS.md es la fuente canónica.
     Invocar skill mbp-brand-design antes de implementar tokens visuales. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens, estilos globales y BaseLayout accesible con lang=es</name>
  <read_first>
    - .planning/content/MBP-BRAND-ANALYSIS.md (FUENTE CANÓNICA — leer ANTES que el UI-SPEC; sección "CSS VARIABLES MÍNIMAS" y "COMPONENTES DETALLADOS")
    - Invocar skill mbp-brand-design para confirmar tokens antes de escribir tokens.css
    - .planning/phases/01-public-mvp/01-UI-SPEC.md (Layout, Spacing, ARIA — ignorar sus valores de color; usar los de MBP-BRAND-ANALYSIS.md)
  </read_first>
  <files>src/styles/tokens.css, src/styles/global.css, src/layouts/BaseLayout.astro</files>
  <action>
    Crear `src/styles/tokens.css` con TODAS las custom properties. Para colores usar
    EXCLUSIVAMENTE la paleta del PDF de branding (MBP-BRAND-ANALYSIS.md):
    --color-primary: #2D320F   /* oliva oscuro — texto, botones, fondos oscuros */
    --color-lavender: #DCCDFF  /* lavanda — hero, fondo logo */
    --color-sage: #C8CD91      /* sage green — sección servicios */
    --color-teal: #87AAAF      /* teal muted — acentos, iconos */
    --color-cream: #F0F0E6     /* crema — fondo neutro principal */
    --color-white: #FFFFFF     /* blanco — cards internas */
    --color-destructive: #C97070 /* rojo desaturado para errores — no existe en paleta, usar con moderación */
    NO definir --color-accent #C17A4A ni --color-surface #FAF7F2 ni --color-text #3B2A1A
    (pertenecen al UI-SPEC desactualizado, no al branding real).
    Tipografía: --font-sans: 'Josefin Sans', sans-serif (Google Fonts, pesos 100/300/400).
    Tracking: --tracking-label: 0.45em; --tracking-heading: 0.35em; --tracking-button: 0.3em; --tracking-body: 0.02em.
    Radios: --radius-pill: 9999px; --radius-card: 14px; --radius-container: 22px.
    Escala de espaciado (--space-xs..--space-3xl), breakpoints (--bp-sm/md/lg) y --max-content: 1280px.
    Logo: implementar como SVG placeholder inline hasta recibir el SVG definitivo del diseñador
    (BLOQUEANTE confirmado — ver MBP-BRAND-ANALYSIS.md sección "Información Pendiente").
    Crear `src/styles/global.css` importando tokens: reset minimo, body con --font-sans y
    --color-text sobre --color-surface, foco visible global `outline: 3px solid
    var(--color-accent); outline-offset: 2px` en `:focus-visible`, y envolver TODA transicion
    en `@media (prefers-reduced-motion: no-preference)` (sin movimiento por defecto). Crear
    `src/layouts/BaseLayout.astro`: `<html lang="es">` (OBLIGATORIO, flag Gemini), skip link
    con texto exacto "Ir al contenido principal" (oculto con left:-9999px, visible en
    focus-visible), `<main id="main-content">`, slots para Header y Footer, y landmarks
    header/nav/main/footer.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('src/layouts/BaseLayout.astro','utf8'); if(!/lang=\"es\"/.test(f))throw new Error('falta lang=es'); if(!/main-content/.test(f))throw new Error('falta main-content'); if(!/Ir al contenido principal/.test(f))throw new Error('falta skip link'); const t=require('fs').readFileSync('src/styles/tokens.css','utf8'); if(!/--color-primary/.test(t))throw new Error('faltan tokens MBP'); if(/--color-accent.*#C17A4A/.test(t))throw new Error('tokens del UI-SPEC desactualizado detectados — usar paleta MBP'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - BaseLayout.astro contiene literal `lang="es"` en el elemento html
    - BaseLayout.astro contiene `id="main-content"` y el skip link "Ir al contenido principal"
    - tokens.css define --color-primary (#2D320F), --color-lavender, --color-sage, --color-teal, --color-cream y la escala de espaciado completa (paleta MBP — NO #C17A4A)
    - global.css define `:focus-visible` con outline 3px y envuelve transiciones en prefers-reduced-motion: no-preference
  </acceptance_criteria>
  <done>Sistema de tokens y layout base accesible con lang=es, skip link, foco visible y movimiento reducido por defecto.</done>
</task>

<task type="auto">
  <name>Task 2: Header, Footer con disclaimer persistente, PlaceholderNotice y E2E</name>
  <read_first>
    - .planning/phases/01-public-mvp/01-UI-SPEC.md (Component Inventory, Copywriting Contract, ARIA)
    - src/layouts/BaseLayout.astro (creado en Task 1)
  </read_first>
  <files>src/components/Header.astro, src/components/Footer.astro, src/components/PlaceholderNotice.astro, tests/e2e/layout.spec.ts</files>
  <action>
    Crear `src/components/Header.astro`: nav horizontal en >=lg, toggle hamburger en movil
    (unico JS de cliente permitido, < 1KB inline, sin framework), fondo --color-nav, area de
    toque >=44px. Crear `src/components/Footer.astro` con fondo --color-footer y texto
    --color-footer-text, e incluir el DISCLAIMER CLINICO PERSISTENTE con el texto exacto del
    UI-SPEC: "Este sitio es solo informativo. No envie sintomas, diagnosticos, resultados ni
    documentos medicos por ningun canal de contacto. En caso de urgencia, acuda al servicio de
    emergencias mas cercano." dentro de `<aside aria-label="Aviso importante">` (NO
    role="alert"). Este disclaimer es elemento visible en cada pagina, no metadata (cierra
    concern de visibilidad del disclaimer, Ley 23/1981). Crear
    `src/components/PlaceholderNotice.astro`: banner con icono SVG inline aria-hidden,
    texto en --color-text, usado para indicar datos pendientes de forma comprensible. Integrar
    Header y Footer en BaseLayout. Escribir `tests/e2e/layout.spec.ts` (Playwright) que: navega
    por teclado (Tab) y activa el skip link; verifica que el disclaimer del footer es visible
    en una pagina; verifica el toggle del menu movil; verifica responsive en viewport movil y
    desktop.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('src/components/Footer.astro','utf8'); if(!/No envie sintomas/.test(f))throw new Error('falta disclaimer'); if(!/aria-label=\"Aviso importante\"/.test(f))throw new Error('falta aside'); console.log('ok')" && npx playwright test tests/e2e/layout.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Footer.astro contiene el texto exacto del disclaimer clinico dentro de `<aside aria-label="Aviso importante">`
    - Header.astro tiene nav con aria-label y toggle movil con area de toque >=44px
    - PlaceholderNotice.astro usa SVG decorativo con aria-hidden="true"
    - tests/e2e/layout.spec.ts pasa: skip link activable por teclado, disclaimer visible, toggle movil, responsive
  </acceptance_criteria>
  <done>Header/Footer/PlaceholderNotice implementados; disclaimer clinico visible persistente en footer; E2E de layout y teclado verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante -> sitio | el visitante podria intentar enviar datos clinicos por canales de contacto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | datos clinicos enviados por visitante | mitigate | disclaimer clinico visible persistente en footer de cada pagina (Ley 23/1981) |
| T-01-10 | Denial of Service | JS de cliente excesivo | accept | unico JS es toggle hamburger < 1KB inline; sin framework |
</threat_model>

<verification>
- E2E de layout verde; skip link y teclado funcionan.
- Disclaimer visible en footer renderizado.
- Sin movimiento con prefers-reduced-motion activo.
</verification>

<success_criteria>
- lang="es" presente (WCAG/SEO).
- Disclaimer clinico persistente y visible (no metadata).
- Accesibilidad de foco, skip link y movimiento reducido.
</success_criteria>

<output>
Crear `.planning/phases/01-public-mvp/01-03-SUMMARY.md` al terminar.
</output>
