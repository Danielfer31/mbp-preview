---
phase: 01-public-mvp
plan: 06
type: execute
wave: 4
depends_on: [01-04, 01-05]
files_modified:
  - playwright.config.ts
  - lighthouserc.json
  - .github/workflows/ci.yml
  - .github/workflows/secret-scan.yml
  - package.json
  - tests/e2e/a11y.spec.ts
autonomous: true
requirements: [QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, WEB-04, WEB-05, PRV-04, OPS-04]
user_setup: []

must_haves:
  truths:
    - "CI ejecuta validate-content, check-hardcoded, unit, e2e, axe y lighthouse en cada cambio"
    - "build:preview pasa con placeholders; build:production falla mientras existan bloqueantes"
    - "El workflow usa permisos de minimo privilegio y acciones fijadas por SHA"
    - "Existe secret scanning para impedir secretos en Git"
    - "Lighthouse mide LCP/INP-proxy/CLS como proxy de laboratorio antes de publicar"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Pipeline CI completo y endurecido"
      contains: "validate-content"
    - path: ".github/workflows/secret-scan.yml"
      provides: "Escaneo de secretos en CI"
      min_lines: 5
    - path: "lighthouserc.json"
      provides: "Presupuestos de rendimiento como proxy de laboratorio"
      contains: "assert"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/validate-content.mjs"
      via: "step run validate production"
      pattern: "validate-content"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-hardcoded-content.mjs"
      via: "step run check"
      pattern: "check-hardcoded"
---

<objective>
Completar la calidad automatizada extendiendo el CI base (de 01-01) con validacion de
contenido, anti-hardcodeo, e2e, axe y lighthouse, y endurecer GitHub Actions.

Cierra concerns MEDIUM: hardening de GitHub Actions (permisos minimos, SHAs fijados, secret
scanning, politica de dependencias), Lighthouse como proxy de laboratorio explicito para INP
(no se afirma medicion de campo), y consolidacion de scripts build:preview/build:production.

Purpose: cada cambio se valida automaticamente y ningun hallazgo alto o contenido no
aprobado puede integrarse.

Output: configs de test/lighthouse, CI completo endurecido, secret-scan y scripts npm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-public-mvp/01-SPEC.md
@.planning/phases/01-public-mvp/01-01-SUMMARY.md
@.planning/phases/01-public-mvp/01-04-SUMMARY.md
@.planning/phases/01-public-mvp/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scripts npm, configs de e2e/axe/lighthouse</name>
  <read_first>
    - .planning/phases/01-public-mvp/01-UI-SPEC.md (Performance Contract: LCP<2.5s, INP<200ms, CLS<0.1)
    - package.json (scripts existentes de 01-01)
    - .planning/phases/01-public-mvp/01-REVIEWS.md (Lighthouse no prueba INP de campo; usar proxy de laboratorio)
  </read_first>
  <files>playwright.config.ts, lighthouserc.json, tests/e2e/a11y.spec.ts, package.json</files>
  <action>
    Configurar `playwright.config.ts` (chromium, viewport movil y desktop, baseURL del
    preview). Crear `tests/e2e/a11y.spec.ts` que ejecute axe-core sobre cada ruta publica y
    falle ante violaciones serias/criticas (WCAG AA). Configurar `lighthouserc.json` con
    assertions de laboratorio: LCP < 2500ms, CLS < 0.1, y TBT como proxy de INP (documentar en
    comentario que INP de campo se difiere a medicion privada futura — concern MEDIUM Codex).
    Anadir/consolidar scripts en package.json: `validate:preview` (node validate-content.mjs
    preview), `validate:production` (node validate-content.mjs production),
    `check:hardcoded` (node check-hardcoded-content.mjs), `test:e2e` (playwright test),
    `build:preview` (validate:preview && astro build), `build:production`
    (validate:production && check:hardcoded && astro build), `lhci` (lhci autorun),
    `verify` (audit && validate:preview && check:hardcoded && test && test:e2e && build:preview).
  </action>
  <verify>
    <automated>npm run build:preview && (npm run build:production; test $? -ne 0) && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - package.json define build:preview, build:production, validate:preview, validate:production, check:hardcoded, test:e2e, lhci y verify
    - build:preview sale 0 con placeholders presentes
    - build:production sale != 0 mientras existan bloqueantes (encadena validate:production y check:hardcoded antes de astro build)
    - lighthouserc.json contiene assertions para LCP, CLS y TBT (proxy de INP) con comentario sobre INP de campo
    - tests/e2e/a11y.spec.ts ejecuta axe en cada ruta y falla ante violaciones serias/criticas
  </acceptance_criteria>
  <done>Scripts y configs de calidad listos; build:production bloquea por contenido y hardcodeo; axe y lighthouse configurados.</done>
</task>

<task type="auto">
  <name>Task 2: CI completo endurecido + secret scanning</name>
  <read_first>
    - .github/workflows/ci.yml (base de 01-01)
    - .planning/phases/01-public-mvp/01-REVIEWS.md (hardening: permisos minimos, SHAs, secret scanning, politica de dependencias)
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/secret-scan.yml, package.json</files>
  <action>
    Extender `.github/workflows/ci.yml` para ejecutar, ademas de audit/build de 01-01:
    `npm run validate:preview`, `npm run check:hardcoded`, `npm test`, `npm run test:e2e` y
    `npm run lhci`. Mantener `permissions: contents: read` a nivel workflow (anadir
    `pull-requests: read` solo si necesario). Confirmar que TODAS las acciones de terceros
    siguen fijadas por SHA de 40 hex. Subir reportes (lighthouse, playwright) como artefactos
    sin datos personales. Crear `.github/workflows/secret-scan.yml` que ejecute un escaneo de
    secretos (gitleaks fijado por SHA, o el escaneo nativo) en push/PR con
    `permissions: contents: read`. Documentar en package.json (campo de comentario o README
    referenciado) la politica de actualizacion trimestral de dependencias (OPS-04). NO incluir
    el step de deploy a Cloudflare aqui — eso vive en 01-09 con sus propios controles.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); for(const s of ['validate:preview','check:hardcoded','test:e2e']) if(!y.includes(s)) throw new Error('falta '+s); if(/uses:\s*\S+@v\d/.test(y)) throw new Error('accion no fijada por SHA'); const s=require('fs').readFileSync('.github/workflows/secret-scan.yml','utf8'); if(!/permissions/.test(s)) throw new Error('secret-scan sin permissions'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml ejecuta validate:preview, check:hardcoded, test, test:e2e y lhci
    - ci.yml mantiene permissions de minimo privilegio y todas las acciones por SHA (no @vN)
    - secret-scan.yml existe, corre en push/PR y usa permissions: contents: read
    - Los reportes se suben como artefactos
    - La politica trimestral de dependencias (OPS-04) queda documentada
  </acceptance_criteria>
  <done>CI completo y endurecido con validacion de contenido, anti-hardcodeo, e2e, axe, lighthouse y secret scanning; deploy excluido (vive en 01-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commits -> repo | secretos podrian filtrarse en Git |
| CI -> artefactos | reportes no deben contener datos personales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | Information Disclosure | secretos en Git (PRV-04) | mitigate | secret-scan.yml en push/PR |
| T-01-19 | Tampering | contenido no aprobado integrado | mitigate | validate:production + check:hardcoded como gates de build |
| T-01-20 | Elevation | permisos amplios del workflow | mitigate | permissions: contents: read; acciones por SHA |
| T-01-SC | Tampering | acciones de terceros | mitigate | SHAs fijados; gitleaks fijado por SHA |
</threat_model>

<verification>
- CI verde con todos los gates.
- build:production falla hoy (bloqueantes activos) — comportamiento esperado.
- secret-scan corre sin hallazgos.
</verification>

<success_criteria>
- Pipeline completo y endurecido (QA-02/04/05/06/07).
- Lighthouse como proxy de laboratorio documentado.
- Secret scanning activo.
</success_criteria>

<output>
Crear `.planning/phases/01-public-mvp/01-06-SUMMARY.md` al terminar.
</output>
